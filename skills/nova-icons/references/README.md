# Nova Icons References

This skill is split into focused reference files so you can load only the icon guidance that matches the work. These references cover Material Icons, Kendo SVG icons, and custom SVG assets in Nova.

## Route By Task

- Use [icon-systems.md](icon-systems.md) for choosing between Material, Kendo, and custom SVG icons
- Use [sizing-and-patterns.md](sizing-and-patterns.md) for icon sizing, icon buttons, and common usage patterns

## Cross-Skill Boundaries

- Use `nova-components` for broader component markup patterns
- Use `nova-styling` for SCSS mixins behind icon sizing helpers
