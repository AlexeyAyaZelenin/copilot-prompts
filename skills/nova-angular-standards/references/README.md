# Nova Angular Standards References

This skill is split into focused reference files so you can load only the broad Angular conventions that match the task. These references cover component authoring, templates and forms, and general code quality rules.

## Route By Task

- Use [component-authoring.md](component-authoring.md) for standalone components, DI, signals, inputs, outputs, and service patterns
- Use [templates-and-forms.md](templates-and-forms.md) for templates, `@defer`, tracking, and typed forms
- Use [code-quality-and-rxjs.md](code-quality-and-rxjs.md) for code quality, import conventions, RxJS lifecycle, and error handling

## Cross-Skill Boundaries

- Use `nova-control-flow`, `nova-signals`, `nova-deferrable-views`, `nova-standalone-migration`, and `nova-ssr-hydration` for deeper guidance in those specific areas
