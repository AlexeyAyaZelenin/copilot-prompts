# Figma To Angular References

This skill is split into focused reference files so you can load only the guidance that matches the implementation step. These references focus on Nova-specific design-to-Angular translation.

## Route By Task

- Use [checklist-and-validation.md](checklist-and-validation.md) for implementation, compile, test, responsive, and accessibility checks
- Use [token-and-component-mapping.md](token-and-component-mapping.md) for color, typography, spacing, and component mapping from Figma to Nova

## Cross-Skill Boundaries

- Use `design-tokens`, `nova-components`, `nova-styling`, and `nova-icons` for the deeper implementation details behind each mapping
