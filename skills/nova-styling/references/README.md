# Nova Styling References

This skill is split into focused reference files so you can load only the styling guidance that matches the work. These references cover Nova SCSS usage, responsive layout, and Material override boundaries.

## Route By Task

- Use [scss-basics.md](scss-basics.md) for imports, component style structure, and styling do and do not rules
- Use [responsive-layout.md](responsive-layout.md) for breakpoints, flex-layout utilities, and responsive layout patterns
- Use [mixins-and-overrides.md](mixins-and-overrides.md) for flexbox mixins, icon sizing, color opacity, Material overrides, and global style organization

## Cross-Skill Boundaries

- Use `design-tokens` for the actual token values
- Use `nova-components` for component-level markup patterns
