# Nova Deferrable Views References

This skill is split into focused reference files so you can load only the defer guidance that matches the task. These references target Angular v17 deferrable views in Nova.

## Route By Task

- Use [loadability-and-triggers.md](loadability-and-triggers.md) for defer-loadable dependencies, trigger types, and trigger selection heuristics
- Use [placeholders-ssr-and-testing.md](placeholders-ssr-and-testing.md) for placeholders, loading and error blocks, SSR behavior, pitfalls, and testing

## Cross-Skill Boundaries

- Use `nova-ssr-hydration` when the main issue is SSR or hydration, not defer itself
